using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum CarState { PARK, BRAKE, DRIVE, REVERSE, DRIFT, DRIFTPOWER };
public enum ItemEffect { NONE, RETURN, SPEEDUP, BOMB, OIL, FLY};